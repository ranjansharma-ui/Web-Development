
import './App.css'

function App() {
  const [count, setCount] = useState(0)

  return (
   <div>
    <nav>
      <button onClick={() => scrollToIndex(0)}>
        cat</button>
        
    </nav>
   </div>
  )
}

export default App
